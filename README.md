# TrailAgent

A simple Streamlit app to visualize trails from a CSV of latitude and longitude points and compute total trail distance.

Usage

1. Create a virtual environment and install dependencies:

   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt

2. Run the app:

   streamlit run streamlit_app.py
